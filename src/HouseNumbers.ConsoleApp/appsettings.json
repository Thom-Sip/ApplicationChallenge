{
  "ParseSettings": {
    "FileName": "dataset_Assessment_Dev_MA_25032024.csv",
    "AllowDuplicates": false,
    "ColumnDelimiterType": "Semicolon", // Semicolon, Tabs, Both
    "SuffixValidationType": "Static", // Static, Regex
    "StaticSuffixValidation": { // Ignored when 'SuffixValidationType == Regex'
      "AllowedCharacters": "ABCDEFGHIJKLMNOPQRSTUVWXYZ",
      "MaxCharacters": 2
    },
    "RegexSuffixValidation": { // Ignored when 'SuffixValidationType == Static'
      "Regex": "^[A-Za-z]{1,2}$" // 1 or 2 characters must be a-z may be capitalized
    }
  },
  "SortingSettings": {
    "Type": "QuickSortService" // BubbleSortService, QuickSortService
  }
}